# MNEMO-03 — Foundation: Magic-link auth

Phase 3 (see `MNEMO-00-ROADMAP.md`). Goal: passwordless magic-link login — request a link by email, click
to establish a session, with auth middleware protecting routes. Depends on MNEMO-01 (Worker) and MNEMO-02
(`accounts` table + `src/db`). Per `docs/PRD.md` v0.5 §6.1 + §8.3: magic-link via Resend, time-limited
single-use tokens, sessions in KV. One account → many agents.

Conventions: tokens are single-use and short-lived; store only a hash of the token, never the raw token.
Sessions are opaque IDs in KV with a TTL. Secrets (`RESEND_API_KEY`) come from Wrangler secrets, not code.

- [x] In `wrangler.toml`, add a KV namespace binding `SESSIONS` (placeholder id with a `# wrangler kv namespace create SESSIONS` comment) and add `SESSIONS: KVNamespace` plus `RESEND_API_KEY: string` and `APP_BASE_URL: string` to the `Env` interface in `src/env.ts`.
  > Done. Repurposed the prior `IDENTITY` KV stub in `wrangler.toml` into an active `[[kv_namespaces]]` binding named `SESSIONS` (placeholder id + `wrangler kv namespace create SESSIONS` comment). Added the three fields to `Env` in `src/env.ts`. Also injected `RESEND_API_KEY`/`APP_BASE_URL` test values into `vitest.config.ts` miniflare bindings so routes have an env to read (the Resend fetch is stubbed in tests).

- [x] Create `src/auth/tokens.ts`: `issueMagicToken(env, email)` generates a random token (`crypto.getRandomValues`), stores `SHA-256(token)` → `{ email, exp }` in KV under a `magic:<hash>` key with a 15-minute TTL, and returns the raw token; `consumeMagicToken(env, token)` looks up the hash, deletes it (single-use), and returns the email if unexpired, else null.
  > Done. 32-byte (`getRandomValues`) hex token; only `SHA-256(token)` is stored under `magic:<hash>` with a 900s `expirationTtl`. `consumeMagicToken` deletes the key first (so even an expired token can't be replayed), then checks the stored `exp` as defense-in-depth beyond the KV TTL — this `exp` field is the seam the expiry test drives.

- [x] Create `src/email/resend.ts`: `sendMagicLink(env, email, url)` POSTs to the Resend API (`https://api.resend.com/emails`) using `env.RESEND_API_KEY`, with a minimal HTML body containing the magic-link URL. Return ok/err; do not throw on non-2xx — surface a typed result.
  > Done. Returns `SendResult = { ok: true } | { ok: false; error }`. Wraps the `fetch` in try/catch and maps non-2xx to `{ ok: false, error: "resend <status>: <body>" }` — never throws. `from` is a placeholder `Mnemosyne <login@mnemosyne.app>` (commented: must be a Resend-verified domain in production).

- [x] Create `src/auth/sessions.ts`: `createSession(env, accountId)` stores an opaque session id in KV (`sess:<id>` → `{ accountId }`, 30-day TTL) and returns it; `getSession(env, id)` reads it; `destroySession(env, id)` deletes it. Use a `Set-Cookie` helper that sets `mnemo_session` HttpOnly, Secure, SameSite=Lax.
  > Done. Opaque `crypto.randomUUID()` id; `sess:<id>` → `{ accountId }` with a 30-day `expirationTtl`. `setSessionCookie`/`clearSessionCookie` wrap Hono's `hono/cookie` helpers with consistent options (`HttpOnly, Secure, SameSite=Lax, Path=/, Max-Age=30d`); cookie name exported as `SESSION_COOKIE`.

- [x] Create `src/auth/routes.ts` mounting on the Hono app: `POST /auth/request` (body `{ email }` validated with Zod → upsert account via `src/db`, issue token, build `${APP_BASE_URL}/auth/callback?token=...`, send via Resend, always return 200 to avoid email enumeration); `GET /auth/callback?token=` (consume token, find/create account, create session, set cookie, redirect to `/`); `POST /auth/logout` (destroy session, clear cookie). Wire these routes into `src/index.ts`.
  > Done. `authRoutes()` returns a `Hono<AppEnv>` mounted via `app.route("/", authRoutes())` in `src/index.ts`. `/auth/request` validates with `z.email()`, normalizes (trim+lowercase), upserts via a new `findOrCreateAccount` helper added to `src/db/index.ts` (composes existing `getAccountByEmail`/`createAccount`, swallows the UNIQUE-race), always returns `{ ok: true }` 200 — a malformed body is the only non-200 (400), which is bad-input not an enumeration signal; Resend send failure is logged server-side, never surfaced. `/auth/callback` consumes the token (400 on invalid/expired), find-or-creates the account, opens a session, sets the cookie, redirects to `/`. `/auth/logout` revokes + clears.

- [x] Create `src/auth/middleware.ts`: a Hono middleware `requireAuth` that reads the `mnemo_session` cookie, loads the session from KV, and sets `c.set("accountId", ...)` on success or returns 401 JSON otherwise. Export a typed helper to read the current account id in handlers.
  > Done. `requireAuth()` returns a `MiddlewareHandler<AppEnv>`; 401 `{ error: "unauthorized" }` when no cookie or unknown/expired session, else stashes `accountId`. Exports `getAccountId(c)` (throws if used off a protected route — programmer error) and the shared `AppEnv = { Bindings: Env; Variables: { accountId } }` type used by routes + `src/index.ts` so the context Variables stay typed end-to-end.

- [x] Create `test/auth.test.ts` (vitest workers pool): unit-test `issueMagicToken`/`consumeMagicToken` (single-use: second consume returns null; expired returns null) and `createSession`/`getSession`/`destroySession`; mock or stub the Resend fetch so no real email is sent. Add a route test that an unauthenticated request to a `requireAuth`-protected test route returns 401.
  > Done. 13 tests: token single-use + unknown + expired (a planted past-`exp` entry, asserting consume returns null AND deletes the key); session create/get/destroy + unknown id; `sendMagicLink` ok-on-2xx (asserts the POST target) and error-without-throw on 422 — Resend `fetch` stubbed via `vi.spyOn(globalThis, "fetch")`, so no email is sent; full `/auth/request`→`/auth/callback`→cookie→protected-route flow (asserts the HttpOnly/Secure/SameSite=Lax cookie unlocks a `requireAuth` route); and the required 401 for an unauthenticated request (plus a 401 for an unknown-session cookie).

- [x] Run `npm run test`, `npm run typecheck`, and `npm run lint`; fix until all pass and report output.
  > All green. `npm run test` → 3 files / 18 tests passed. `npm run typecheck` (`tsc --noEmit`) → no errors (fixed one test-only generic: the protected test app now uses the shared `AppEnv` type instead of `typeof env`, which carries the test-only `TEST_MIGRATIONS` binding). `npm run lint` (`biome check .`) → exit 0, "Checked 20 files. No fixes applied." after applying Biome's safe import-order/format fixes to `src/index.ts`, `src/auth/tokens.ts`, `src/email/resend.ts`, `test/auth.test.ts`. (Note: the RTK shell proxy garbles Biome's console output as "ESLint output"; the underlying `biome check` exits 0.)
