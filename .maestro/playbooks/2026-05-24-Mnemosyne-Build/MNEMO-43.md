# MNEMO-43 — Frontend: Mobile-responsive parity pass (across all screens)

- [x] **Design-system reuse (do this before any responsive work):** all responsiveness must come from the shared component library and design tokens in MNEMO-32-b — use the shared layout primitives, `AppShell`, breakpoint tokens, and any shared responsive helpers rather than ad-hoc media queries or one-off styled elements. Do NOT introduce bespoke controls or raw interactive HTML (`<button>`, `<input>`, `<select>`, `<textarea>`, action `<a>`) while adjusting layouts; if a responsive primitive is missing, add it to `frontend/src/components/ui/` (with a catalog entry) and reuse it everywhere. Run `npm run lint`; MNEMO-32-b's rule fails the build on raw interactive elements outside `components/ui/`.
  > Done: all work reuses the design tokens (`--bp-*`, new `--touch-target-min`) + shared primitives. The new responsive primitives live in `frontend/src/components/layout/` (per the next task's explicit path) and are added to the catalog (`pages/dev/Components.tsx`); their only interactive element is the shared `Button` (back control), so no raw interactive HTML was introduced anywhere. `npm run lint` (biome) passes clean (249 files, 0 issues).

Phase 43, closing Track G — Frontend (see `MNEMO-00-ROADMAP.md`). Goal: a **mobile-responsive parity
pass** so every screen built in MNEMO-32–42 is fully usable and feature-complete on phone-sized viewports —
not a degraded experience. Depends on the prior frontend phases: **MNEMO-32** (scaffold/design system),
**33** (auth), **34** (agent list + create wizard), **35** (conversation), **36** (agent detail tabs),
**37** (glass cockpit), **38** (brain explorer), **39** (brain versioning), **40** (brain graph), **41**
(reports), **42** (dashboard). Per `docs/PRD.md` v0.5 §6.5: mobile-responsive with **feature parity**; this
is a hard requirement across the app.

Conventions: this is primarily a refactor/hardening phase — adjust layouts, not features. Establish shared
responsive primitives once and apply them everywhere, rather than ad-hoc media queries per screen. Target a
**mobile-first** approach using the design-system breakpoints from MNEMO-32 (define them if MNEMO-32 left
them implicit). Two-pane "master/detail" layouts (explorer/editor in MNEMO-38, history in MNEMO-39, reports
list/viewer in MNEMO-41, dashboard grid) collapse to a single column with navigable detail views on small
screens. Touch targets ≥ 44px; no horizontal overflow. Add automated viewport assertions where practical;
true visual QA is a human checkpoint at the end. Tests use vitest + @testing-library/react.

- [x] Establish shared responsive primitives in `frontend/src/components/layout/`: confirm or define breakpoint tokens (e.g. `sm`/`md`/`lg`) and create a `useBreakpoint()` / `useIsMobile()` hook (matchMedia-based, SSR-safe, with a test seam) and a `<ResponsiveMasterDetail>` component that renders both panes side-by-side at `>= md` and, on mobile, shows the master list with a push/overlay detail view plus a back affordance. If MNEMO-32 already provides equivalents, reuse them and only fill gaps. Export these from a layout index so all screens import one source of truth.
  > Done: breakpoint tokens already existed (`--bp-*` in `tokens.css`, `BREAKPOINTS` in `tokens.ts`) — reused. Added `components/layout/useBreakpoint.ts` (`useMediaQuery`/`useBreakpoint`/`useIsMobile` + `atLeast`/`below` query builders; matchMedia-based, SSR/jsdom-safe — returns `false` when `matchMedia` is absent — driven by a `window.matchMedia` test seam) and `components/layout/ResponsiveMasterDetail.tsx` (+ `.module.css`): both panes at `>= md`, single push view with a shared-`Button` back control below `md`. All exported from `components/layout/index.ts` (which also re-exports the existing `AppLayout`).

- [x] Make the app shell/navigation responsive in the top-level layout (the shell from MNEMO-32/36): collapse the primary nav into a hamburger/drawer on mobile, ensure the agent-detail **tab bar** (MNEMO-36: Chat/Brain/Graph/Reports/Audit/Settings) scrolls horizontally or wraps without overflow on narrow viewports, and make the header sticky with adequate touch targets. Keep desktop layout unchanged at `>= md`.
  > Done: the shell already collapsed correctly (MNEMO-32 `AppShell` turns the sidebar into an off-canvas drawer + `TopBar` hamburger below `md`; `RoutedTabs` `.tablist` already `overflow-x: auto`; `AppShell .header` is `position: sticky`). The remaining gap was touch targets: added a `--touch-target-min` (44px) token and applied a `min-height`/size bump on mobile (`max-width: 767px`) to `Input`/`Select`/`Button`(md,lg)/`IconButton`(md,lg) — so the hamburger and header controls are ≥44px. All gated to mobile, so desktop (`>= md`) is unchanged.

- [x] Apply `<ResponsiveMasterDetail>` (or single-column collapse) to the brain explorer (`frontend/src/pages/agent/BrainExplorerTab.tsx`, MNEMO-38) and brain versioning (`frontend/src/components/brain/BrainHistoryPanel.tsx`, MNEMO-39): on mobile, show the file tree / commit list first, then push to the editor / diff viewer with a back button. Ensure the `DiffViewer` lines wrap or scroll horizontally within the viewport (no page-level overflow) and that the `FileEditor` textarea and toolbar fit mobile width.
  > Done: both screens now compose `<ResponsiveMasterDetail>`. BrainExplorer drives `showDetail` from `selectedPath` (tree first; selecting a file pushes the editor). BrainHistory auto-selects the newest commit, so it uses an explicit `viewingDiff` state set on commit-click (commit list first; choosing a commit pushes the diff). `DiffViewer` already scrolls horizontally internally (`.body { overflow-x: auto }` + `.lineText { white-space: pre }`) and the detail pane has `min-width: 0`, so no page-level overflow; the `FileEditor` textarea is `fullWidth` and its toolbar fits. Dead `.panes` media queries removed from the two CSS modules.

- [x] Make the report viewer (`frontend/src/pages/agent/ReportsTab.tsx`, MNEMO-41) responsive: collapse search+list / viewer into single-column master-detail on mobile; ensure rendered markdown wraps, tables scroll horizontally inside a container, and **embedded PNG charts scale to viewport width** (`max-width: 100%`, auto height) without breaking layout. Verify the front-matter header chips wrap.
  > Done: ReportsTab now uses `<ResponsiveMasterDetail>` (`showDetail` from `selectedId`; list first, selecting a report pushes the viewer). On mobile the master/detail back owns return-to-list, so `ReportViewer`'s own "Back" (`onClose`) is suppressed on mobile to avoid a duplicate. Markdown already wraps (`overflow-wrap: anywhere`); embedded charts already scale (`.image { max-width: 100%; height: auto }`). NEW: wrapped GFM tables in a `.tableScroll` (`overflow-x: auto`) container via a custom `table` renderer in `MarkdownRenderer`, so wide tables scroll instead of overflowing. Front-matter tag chips already wrap (`<Inline wrap>` in `FrontMatterHeader`).

- [x] Make the brain graph (`frontend/src/pages/agent/BrainGraphTab.tsx` + `BrainGraphCanvas.tsx`, MNEMO-40) responsive: size the canvas to its container (ResizeObserver-driven width/height) so it fits mobile width, enable touch gestures (pinch-zoom/pan/tap-to-select) on the chosen graph lib, and ensure the brain-size badge + refresh control sit above the canvas without overlap on small screens.
  > Done: `BrainGraphCanvas` already sizes via `ResizeObserver` (canvas fills container, reflows on resize → fits mobile). Made touch parity explicit: set `enablePointerInteraction`/`enableZoomInteraction`/`enablePanInteraction` on `ForceGraph2D` (pinch-zoom/pan/tap-to-select; defaults, now intent-locked) and added `touch-action: none` on the canvas container so gestures go to d3-zoom instead of the browser. Badge + Refresh already sit in a wrapping `<Inline>` row in a `Stack` above the canvas `Panel` (no overlap on small screens).

- [x] Make the conversation (MNEMO-35) and glass-cockpit (MNEMO-37) screens responsive: the chat composer stays pinned and thumb-reachable with the message list scrolling above it; the audit stream filters (type/level/session/time) collapse into a filter sheet/drawer on mobile and the "show the work" toggle remains reachable. Verify long audit/chat content does not cause horizontal overflow.
  > Done — verified already responsive from MNEMO-35/37; no changes needed beyond the global touch-target bump. `Composer` is `position: sticky; bottom: 0` with `env(safe-area-inset-bottom)` and the `MessageList` scrolls above it. `AuditFilterBar` already collapses its controls into a `Drawer` (filter sheet) below `md` via a "Filters" trigger; the `AltitudeToggle` ("show the work") lives in the cockpit's wrapping `headerTop` and stays reachable. Long content cannot overflow horizontally: `MessageBubble` uses `overflow-wrap: anywhere` + `max-width: min(46rem, 80%)` (code blocks `overflow-x: auto`); `AuditEventRow` uses `overflow-wrap: anywhere` / `pre-wrap` + internal `overflow: auto`.

- [x] Make the dashboard (`frontend/src/pages/DashboardPage.tsx`, MNEMO-42) and auth/create-wizard (MNEMO-33/34) responsive: the agent-card grid reflows to a single column on mobile; `AgentFilters` controls stack and remain usable; the create wizard's steps and the magic-link auth forms are single-column, full-width, with inputs ≥ 44px tall. No horizontal scroll.
  > Done: the dashboard `Grid` now takes an explicit single-column branch on mobile (`columns={1}`, dropping the desktop `minColumnWidth="18rem"` auto-fit) via `useIsMobile` — a JS branch the new test asserts. `AgentFilters` already stacks (`flex-direction: column` ≤640px) and the cards reflow. The `LoginPage` card (max-width 26rem) and `CreateAgentWizard` panel (max-width 40rem) are already single-column/full-width; their `Input`/`Textarea` are `fullWidth`, and the `--touch-target-min` (44px) mobile `min-height` on `Input`/`Select` (+ md/lg `Button`) makes the auth/wizard fields and the "Send magic link"/"Begin" buttons ≥44px tall.

- [x] Create `frontend/src/components/layout/__tests__/responsive.test.tsx`: unit-test the `useBreakpoint`/`useIsMobile` hook by stubbing `matchMedia` for mobile vs. desktop widths, and test `<ResponsiveMasterDetail>` — at desktop it renders both master and detail; at mobile it renders the master and, after selecting an item, renders the detail with a working back control that returns to the master. No real layout/canvas.
  > Done: added `responsive.test.tsx` (6 tests). Hook tests stub `matchMedia` via the new `src/test/matchMedia.ts` seam (width-aware: evaluates min/max-width queries against 375px mobile / 1024px desktop) — asserting `useIsMobile`/`useBreakpoint("md")` for both widths plus the no-`matchMedia` (SSR/jsdom) default. `<ResponsiveMasterDetail>` tests: desktop renders both panes; mobile renders the master, a "select" click pushes the detail, and the "Back" control returns to the master (master/detail mutually exclusive on mobile).

- [x] Add mobile-viewport rendering assertions for representative screens in `frontend/src/pages/.../__tests__/` (extend existing test files from MNEMO-38/41/42 rather than duplicating data mocks): with `matchMedia` stubbed to mobile, assert the brain explorer renders the tree first (editor not simultaneously visible), the reports tab renders the list first, and the dashboard renders a single-column grid — proving the responsive branch is taken. Keep these light; they assert structure, not pixels.
  > Done: extended the existing `BrainExplorerTab.test.tsx`, `ReportsTab.test.tsx`, and `DashboardPage.test.tsx` (reusing their data mocks; added a shared `stubMatchMedia`/`restoreMatchMedia` seam + `afterEach` restore). With mobile stubbed: brain explorer shows the tree (`notes`) while the editor's "Select a file to view" prompt is absent; reports shows the list (titles) while the viewer's "Select a report" prompt is absent; dashboard's grid (`data-testid="agent-grid"`) has `gridTemplateColumns: repeat(1, minmax(0, 1fr))`. Structure-only, no pixels.

- [x] Add a stylelint/usage guard if cheap: a small test or lint rule asserting no fixed pixel `width`/`min-width` on the top-level page containers that would force horizontal overflow on a 360px viewport (or, if not practical as a rule, a documented checklist comment in the layout index). Do not over-engineer — prefer the runtime tests above.
  > Done: took the sanctioned fallback (a static lint rule for fixed widths was more trouble than value, and runtime viewport tests above are the primary guard). Added a documented 6-point "Mobile no-overflow checklist" comment block to `components/layout/index.ts` (no fixed px width on page containers; fixed widths only on inner panes paired with `max-width: 100%`; overflow content in `overflow-x: auto` + `min-width: 0` containers; images `max-width: 100%`; grids reflow to one column; ≥44px touch targets) — the standard every screen must hold at a 360px viewport.

- [x] From `frontend/`, run `npm run test`, `npm run typecheck`, and `npm run lint`; fix until all pass and report the final command output.
  > Done — all three pass:
  > - `npm run test`: **Test Files 41 passed (41) · Tests 154 passed (154)** (was 145; +9 new: 6 in responsive.test.tsx, +1 each in BrainExplorer/Reports/Dashboard).
  > - `npm run typecheck` (`tsc --noEmit`): clean, exit 0.
  > - `npm run lint` (`biome check .`): **Checked 249 files. No fixes applied.** (0 issues; raw-interactive-element rule satisfied.)
  > - Bonus `npm run build`: compiles (the >500 kB chunk-size warning is pre-existing, unrelated to this phase).
