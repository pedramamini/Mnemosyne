# MNEMO-37 — Frontend: Glass cockpit UI (live audit stream, filters, search, altitude toggle)

- [x] **Design-system reuse (do this before building any UI):** build every part of this screen exclusively from the shared component library in `frontend/src/components/ui/` and the design tokens from MNEMO-32-b — do NOT create bespoke buttons, inputs, selects, modals, tables, or other one-off controls, and do NOT use raw interactive HTML elements (`<button>`, `<input>`, `<select>`, `<textarea>`, action `<a>`). If a primitive you need is missing, add it to `frontend/src/components/ui/` (with a catalog entry in the dev gallery) and consume it from there — never build it locally. Run `npm run lint`; MNEMO-32-b's rule fails the build on raw interactive elements outside `components/ui/`.
  - **Done.** Every audit component is composed from `@/components/ui` (`Button`, `IconButton`, `SearchInput`, `Select`, `Checkbox`, `Drawer`, `Badge`, `Code`, `Spinner`, `EmptyState`, `Panel`, `Stack`/`Inline`/`Text`/`Icon`, `Input` for the custom date range) plus design tokens in every `.module.css`. No raw interactive elements: the segmented altitude toggle is two shared `Button`s in a `radiogroup`; disclosures/load-older/jump-to-live are `Button`s. No new primitive was needed (the segmented control is a small feature composition, not a generic primitive), so the dev gallery is unchanged. `biome check .` (the lint command) passes with zero `noRestrictedElements` violations.

Phase 37 (see `MNEMO-00-ROADMAP.md`). Goal: the "glass cockpit" — a live, filterable, searchable audit
stream that makes the agent's work legible to a non-technical user, anchored by the **altitude toggle**: a
calm plain-English `milestone` stream by default, with a "show the work" switch that reveals the raw
bash/Python/reasoning (`info`/`error`). This is the key UX that makes complex agent machinery attainable to
the general population (`docs/PRD.md` v0.5 §6.7). Depends on MNEMO-22 (Audit API: SSE stream with a
`sinceSeq` cursor for reconnect backfill, filter by type/level/session/time, FTS5 search over the human
summary, altitude `level` field) and MNEMO-36 (agent detail shell + the Audit tab placeholder this phase
fills in). Per §6.7 (event types `session.*`, `source.read`, `memory.wrote/linked/consolidated`,
`tool.authored/ran`, `report.generated`, `chart.rendered`, `narration`, `error`; `level` = `milestone | info | error`;
streamable + filterable + searchable) and §7.4 (dedicated `AuditLog` DO, SSE fan-out).

Conventions: the stream is consumed over SSE with a `sinceSeq` cursor so a reconnect backfills missed
events (never gaps, never dupes — events carry a monotonic `seq`). Audit components live in
`frontend/src/components/audit/`, the audit API/stream client in `frontend/src/api/audit.ts`, and this
phase replaces the `AuditTab` stub from MNEMO-36. Default altitude is `milestone` (calm). Separate
component code / tests / test-runs.

- [x] Create the audit types + REST client `frontend/src/api/audit.ts`: TS types `AuditLevel = "milestone" | "info" | "error"`, `AuditEventType` (union of the §6.7 types), and `AuditEvent { seq: number; ts: string; type: AuditEventType; level: AuditLevel; sessionId: string; summary: string; detail?: { command?: string; code?: string; reasoning?: string; output?: string; [k: string]: unknown } }` (the human `summary` drives the calm view; `detail` carries the raw bash/Python/reasoning for "show the work"). Implement `fetchAuditPage(agentId, { sinceSeq?, type?, level?, sessionId?, from?, to?, limit? })` (`GET` the MNEMO-22 filtered query endpoint, returns `{ events: AuditEvent[]; nextSeq: number }`) and `searchAudit(agentId, query, filters?)` (FTS5 over the summary). Add a comment mapping these to the actual MNEMO-22 routes; this is the single REST adapter point.
  - **Done** (`frontend/src/api/audit.ts`). Declared the idealized `AuditLevel`/`AuditEventType`/`AuditEvent` shapes plus `AuditAltitude = "milestone" | "all"`, `AuditFilters`, and `AuditPage`. `fetchAuditPage` → `GET /agents/:id/audit/events` (maps the bare `BackendAuditEvent[]` to `AuditEvent`, derives `nextSeq` = max seq seen); `searchAudit` → `GET /agents/:id/audit/search`. A header comment documents the wire→UI mapping (`ts` epoch-ms→ISO, `text`→`summary`, `payload`→`detail`, null `sessionId`→`""`) and the altitude/level seam. The shared `toAuditEvent` mapper is exported for the SSE client. **Backend enablement:** MNEMO-22's `level` filter was exact-match defaulting to `milestone` with no "all levels" path, so "Show the work" couldn't fetch everything in one call. Added a backward-compatible `level=all` sentinel to `/events` + `/stream` (`src/audit/routes.ts`, `src/audit/AuditLog.ts`) meaning "no level filter"; omitting `level` still defaults to the calm milestone. Covered by new backend tests (`test/audit-api.test.ts`, `test/audit-stream.test.ts`); all 18 backend audit tests pass.

- [x] Create the SSE stream client `frontend/src/api/auditStream.ts`: `openAuditStream(agentId, { sinceSeq, filters }, onEvent, onStatus)` returning a handle with `close()`. Use the `fetch`+`ReadableStream` SSE reader (so it can send `credentials: "include"` and pass `sinceSeq`/filters as query params — `EventSource` can't set headers/credentials cleanly) parsing `data:` lines into `AuditEvent`s. On disconnect, auto-reconnect with backoff, resuming from the **highest `seq` seen** so the backend backfills the gap via `sinceSeq` (per §6.7). Surface connection status (`connecting | live | reconnecting | closed`) via `onStatus`. Keep parsing tolerant of keep-alive comment lines.
  - **Done** (`frontend/src/api/auditStream.ts`). `openAuditStream` uses `fetch` + a `ReadableStream` reader with `credentials: "include"`, framing on `\n\n` and parsing `data:` lines (tolerant of `:`-prefixed keep-alives and partial frames) through the shared `toAuditEvent`. Tracks the highest `seq` seen and reconnects with exponential backoff (500ms→15s, reset on a clean connect) passing `?sinceSeq=` so MNEMO-22 backfills the gap before resuming live. Emits `connecting | live | reconnecting | closed` via `onStatus`; `close()` aborts the in-flight request and cancels any pending reconnect.

- [x] Create the stream-state hook `frontend/src/components/audit/useAuditStream.ts`: given `agentId` + the active filters + altitude `level`, it (1) loads an initial page via `fetchAuditPage`, (2) opens the live stream from the last seq, (3) merges incoming events deduped + ordered by `seq`, capping the in-memory buffer (e.g. last N, with "load older" paging downward via `fetchAuditPage` cursor). Expose `{ events, status, loadOlder, hasOlder }`. Changing filters/level re-subscribes cleanly (close old handle, reset, reload). The hook owns all lifecycle; components stay declarative.
  - **Done** (`frontend/src/components/audit/useAuditStream.ts`). Loads the initial page, opens the stream from `nextSeq`, merges via a seq-keyed map (dedup + ascending sort), caps the live buffer at 1000 (newest kept), and pages older via the forward cursor. Exposes `{ events, status, loadOlder, hasOlder, loadingOlder, loading }`. Re-subscribes on `agentId`/`level`/`filters` change, closing the prior handle on cleanup. **Robustness:** `filters` is stabilized by a normalized content key (`useMemo`) so a caller passing a fresh `{}` each render can't thrash the subscription into an infinite re-subscribe loop — caught and fixed during testing.

- [x] Create the **altitude toggle** `frontend/src/components/audit/AltitudeToggle.tsx` (+ `.module.css`): a prominent segmented control — "Milestones" (calm, default) vs. "Show the work" (everything). Milestones maps to `level === "milestone"` only; "Show the work" includes `info`/`error` and unlocks the raw `detail` (command/code/reasoning/output) on each event. This is the §6.7 narrated-progressive-disclosure control — make it the visual anchor of the panel, with a one-line helper explaining what each mode shows. Controlled via props (`value`, `onChange`).
  - **Done** (`AltitudeToggle.tsx` + `.module.css`). A pill `radiogroup` of two shared `Button`s with icons: "Milestones" (→ `value="milestone"`, the default) vs. "Show the work" (→ `value="all"`, which the hook/adapter translate to `level=all` so info/error join milestones and rows reveal their `detail`). A one-line helper changes per mode. Controlled via `value`/`onChange`.

- [x] Create the event row + stream list components in `frontend/src/components/audit/`: `AuditEventRow.tsx` (+ `.module.css`) — an icon per `type`, the plain-English `summary`, a relative timestamp, a subtle `level` tint, and (only in "show the work") an expandable `detail` disclosure rendering `command`/`code` in a monospace block, `reasoning` as text, and `output` truncated-with-expand. `AuditStream.tsx` — a virtualized-friendly scrolling list of `AuditEventRow`s, newest at the bottom with auto-follow (pause auto-follow when the user scrolls up; a "jump to live" pill resumes), a connection-status indicator (live/reconnecting), a "load older" trigger at the top, and an `EmptyState` when there are no events yet.
  - **Done** (`AuditEventRow.tsx`/`AuditStream.tsx` + `.module.css`). Row: a per-`type` lucide icon, the `summary`, a relative timestamp (`relativeTime` helper), a left-rail `level` tint + level `Badge`, and (only when `showDetail`) a "Show work"/"Hide work" disclosure rendering `command`/`code` as `Code block`, `reasoning` as `Text`, and `output` truncated-with-expand at 400 chars. Stream: a `role="log"` scroller (keyed by `seq`, virtualization-friendly), auto-follow pinned to the bottom with an `onScroll` follow detector + a "Jump to live" pill, a status `Badge` (live/reconnecting/connecting/disconnected with a `Spinner`), a top "Load older" trigger, and an `EmptyState` for no events.

- [x] Create the filter bar `frontend/src/components/audit/AuditFilterBar.tsx` (+ `.module.css`): controls for **type** (multi-select of the §6.7 event types, grouped sensibly — sources / memory / tools / reports / errors), **session** (dropdown of recent `sessionId`s, "all" default), and **time** (relative presets: last hour / today / 7 days / all, plus a custom range). It owns no data — emits a `filters` object consumed by `useAuditStream`. Keep the `level` out of here (the `AltitudeToggle` owns altitude); type/session/time live here. Mobile: collapses into a "Filters" sheet/popover.
  - **Done** (`AuditFilterBar.tsx` + `.module.css`). Type multi-select via grouped `Checkbox`es (Activity / Sources / Memory / Tools / Reports / Errors), a session `Select` ("All sessions" default, fed recent ids derived from the stream), and a time `Select` (All time / Last hour / Today / Last 7 days / Custom range) with two `datetime-local` `Input`s for the custom window. Owns no data — emits a normalized `AuditFilters` (epoch-ms `from`/`to`) via `onChange`; `level` deliberately absent. Below 768px the inline controls hide and a "Filters (N)" `Button` opens the same controls in a `Drawer` (one source of UI truth).

- [x] Create the FTS search box `frontend/src/components/audit/AuditSearch.tsx` (+ `.module.css`): a debounced search input that calls `searchAudit` (FTS5 over the human summary, §6.7) and switches the stream view into a "search results" mode (paused live-follow, results listed via `AuditEventRow`, clearing the query returns to the live stream). Show result count + a clear button. Keep search results respecting the active altitude (milestone vs. show-the-work detail visibility).
  - **Done** (`AuditSearch.tsx` + `.module.css`). A `SearchInput` (with clear button) debounced 300ms calling `searchAudit`, with a latest-request guard so a slow query can't clobber a newer one. Reports `{ active, query, results, loading }` up so the cockpit swaps the live tail for a results list (and resumes live on clear). Shows "Searching…"/result count. Search spans all altitudes (the backend FTS isn't level-filtered); the active altitude only governs each result row's detail visibility (`showDetail` passed through).

- [x] Assemble the cockpit panel `frontend/src/components/audit/GlassCockpit.tsx` (+ `.module.css`): composes `AltitudeToggle` + `AuditSearch` + `AuditFilterBar` (a sticky header) over the `AuditStream`, wired to `useAuditStream(agentId, filters, level)`. Replace the MNEMO-36 `AuditTab.tsx` stub so `/agents/:agentId/audit` renders `<GlassCockpit agentId={agentId} />`. Default altitude is "Milestones" so a first-time user sees the calm narrated stream; the cockpit should feel readable with zero configuration.
  - **Done** (`GlassCockpit.tsx` + `.module.css`). Composes the altitude toggle + search + filter bar in a sticky `Panel` header over `AuditStream` (or the search-results list when a query is active), wired to `useAuditStream(agentId, filters, altitude)`; session options are derived from the live events. Default altitude is "Milestones" (calm, zero-config). `AuditTab.tsx` now renders `<GlassCockpit agentId={agentId} />` (agentId from `useParams`), replacing the MNEMO-36 stub.

- [x] Create tests (vitest + testing-library): `frontend/src/components/audit/__tests__/AltitudeToggle.test.tsx` — renders both modes, defaults to Milestones, calls `onChange` on switch. `frontend/src/components/audit/__tests__/AuditEventRow.test.tsx` — milestone mode renders only the summary (no raw detail), "show the work" mode renders the expandable `command`/`code`/`reasoning` detail. `frontend/src/components/audit/__tests__/useAuditStream.test.ts` — mock `fetchAuditPage` + `openAuditStream`; assert initial page loads, a streamed event with a higher `seq` is appended, a duplicate `seq` is ignored, and changing the level/filters re-subscribes (old handle closed). `frontend/src/components/audit/__tests__/GlassCockpit.test.tsx` — with the stream hook mocked, milestone events render in the calm view; flipping the altitude toggle reveals detail; typing in search calls a mocked `searchAudit` and shows results.
  - **Done.** All four test files written exactly as specified (12 tests). `AltitudeToggle`: both modes, default Milestones, `onChange("all")` on switch. `AuditEventRow`: calm shows only the summary (no disclosure/raw command), "show the work" expands command/code/reasoning, no disclosure when an event has no detail. `useAuditStream`: initial page loads, higher-seq appended, duplicate-seq deduped, altitude change closes the old handle + opens a new one carrying `level: "all"`. `GlassCockpit`: calm view (no disclosure), flipping the toggle reveals detail, typing in search calls `searchAudit("a1","funding")` and renders results. Backend: 2 new tests for the `level=all` enablement. All 12 frontend + 18 backend audit tests pass.

- [x] Run `npm run typecheck`, `npm run test`, and `npm run lint` inside `frontend/`; fix until all pass and report the final output.
  - **Done — all green.** `npm run typecheck` (`tsc --noEmit`): no errors. `npm run test` (`vitest run`): **75 passed (75)**, 0 failed (the 12 new audit tests plus the existing suite). `npm run lint` (`biome check .`): "Checked 184 files… No fixes applied" — zero errors/warnings, including the `noRestrictedElements` rule (no raw interactive elements outside `components/ui/`). Backend `src/audit` changes verified separately: 18/18 audit tests pass (`test/audit-{api,stream,do}.test.ts`).
